<odoo>
        <template id="mail_reset.ask_form" name="Mail Reset Ask">
            <t t-call="web.login_layout">
            <t t-set="disable_footer" t-value="1"/>
                <div t-if="message">
                    <p class="alert alert-success" t-if="message">
                        <t t-esc="message"/>
                    </p>
                </div>

            <form action="/mail_reset/submit" role="form" method="post">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="form-group field-login">
                    <label for="email" class="control-label">Your Email</label>
                    <input type="text" name="email" t-att-value="email" id="email" class="form-control" autofocus="autofocus"
                        autocapitalize="off" required="required"/>
                </div>
                    <p class="alert alert-danger" t-if="error">
                        <t t-esc="error"/>
                    </p>
                    <div class="clearfix oe_login_buttons">
                        <button type="submit" class="btn btn-primary pull-left">Submit</button>
                    </div>
            </form>
            </t>
        </template>
    
        <template id="mail_reset.reset_password" name="Mail Reset Password">
            <t t-call="web.login_layout">
            <t t-set="disable_footer" t-value="1"/>
                <div t-if="message">
                    <p class="alert alert-success" t-if="message">
                        <t t-esc="message"/>
                    </p>
                </div>
            <form class="mail_reset_password_form" role="form" method="post">
            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
            <div class="form-group field-login">
                <label for="email" class="control-label">Your Email</label>
                <input type="text" name="email" t-att-value="email" id="email" class="form-control" autofocus="autofocus"
                    autocapitalize="off" readonly="1" required="required"/>
            </div>

            <div class="form-group field-name">
                <label for="name" class="control-label">Your Name</label>
                <input type="text" name="name" t-att-value="name" id="name" class="form-control" placeholder="e.g. John Doe"
                    required="required" readonly="1" />
            </div>

            <div class="form-group field-password">
                <label for="password" class="control-label">Password</label>
                <input type="password" name="password" id="password" class="form-control"
                    required="required" t-att-autofocus="'autofocus' if only_passwords else None"/>
            </div>

            <div class="form-group field-confirm_password">
                <label for="confirm_password" class="control-label">Confirm Password</label>
                <input type="password" name="confirm_password" id="confirm_password" class="form-control" required="required"/>
            </div>
            <p class="alert alert-danger" t-if="error">
                <t t-esc="error"/>
            </p>

            <input type="hidden" name="token" t-att-value="token"/>
                    <div class="clearfix oe_login_buttons">
                        <button type="submit" class="btn btn-primary pull-left">Confirm</button>
                    </div>
                </form>
            </t>
        </template>
</odoo>